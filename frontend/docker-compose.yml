  frontend:
    build:
      context: ./frontend
    container_name: wanderly_frontend
    restart: always
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend